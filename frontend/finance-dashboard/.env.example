# Copy this file to .env and adjust values as needed.
# In development (npm start), the CRA proxy forwards /api to the backend so REACT_APP_API_BASE can stay empty.
# In production builds, set the full backend origin, e.g. https://api.example.com
REACT_APP_API_BASE=
